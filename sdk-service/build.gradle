// Apply the java-library plugin to add support for Java Library
plugins {
    id 'java'
}
dependencies {
    compile project(':sdk-amop')
    compile project(':sdk-abi')
    compile ("com.fasterxml.jackson.core:jackson-databind:${jacksonVersion}")
}
